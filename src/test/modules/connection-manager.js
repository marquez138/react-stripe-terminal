import ConnectionManager from '../../modules/connection-manager'
import StripePos from '../../modules/stripe-pos'
describe('#connection-manager', () => {
    describe('#connectToReader', () => {
        it('Successfully connects to a provided reader', () => {
            let manager = new ConnectionManager({})
        })
    })
})